package com.oerms.online_exam_system.entity;

/**
 * Enum representing the pass/fail status of a Result.
 * Stored as a String in the database (EnumType.STRING in Result entity).
 */
public enum ResultStatus {
    PASS,
    FAIL
}
